def camera_offset(frame, x_offset, box_dim):
    for particle_index in range(frame.particles.N):
        print("to do tbh")
    return x_offset


def camera_offset_y(frame, y_offset, box_dim):
    for particle_index in range(frame.particles.N):
        print("to do tbh")
    return y_offset
